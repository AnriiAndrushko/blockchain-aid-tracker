@page "/"
@inject NavigationManager Navigation

<PageTitle>Blockchain Aid Tracker</PageTitle>

<AuthorizeView>
    <Authorized>
        @{ Navigation.NavigateTo("/dashboard", forceLoad: true); }
    </Authorized>
    <NotAuthorized>
        @{ Navigation.NavigateTo("/login", forceLoad: true); }
    </NotAuthorized>
</AuthorizeView>
